<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ace_run.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ace_run"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Ace Run">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Margin="0" RowDefinitions="Auto,*">

        <!-- Title bar area -->
        <Grid Grid.Row="0" Padding="16,12" ColumnDefinitions="*,Auto">
            <TextBlock Text="Ace Run"
                       Style="{StaticResource TitleTextBlockStyle}"
                       VerticalAlignment="Center" />
            <Button Grid.Column="1"
                    x:Name="AddButton"
                    Click="AddButton_Click"
                    Style="{StaticResource AccentButtonStyle}"
                    VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE710;" FontSize="14" />
                    <TextBlock Text="Add" />
                </StackPanel>
            </Button>
        </Grid>

        <!-- App list -->
        <ListView Grid.Row="1"
                  x:Name="AppListView"
                  ItemsSource="{x:Bind Items}"
                  Padding="8,0,8,8"
                  SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:AppItemViewModel">
                    <Grid Padding="8,10" ColumnDefinitions="*,Auto" MinHeight="48"
                          ToolTipService.ToolTip="{x:Bind FilePath}">
                        <Grid.ContextFlyout>
                            <MenuFlyout>
                                <MenuFlyoutItem Text="Edit" Click="EditItem_Click" Tag="{x:Bind Id}">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE70F;" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                                <MenuFlyoutItem Text="Delete" Click="DeleteItem_Click" Tag="{x:Bind Id}">
                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE74D;" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                            </MenuFlyout>
                        </Grid.ContextFlyout>
                        <TextBlock Text="{x:Bind DisplayName, Mode=OneWay}"
                                   FontSize="14" FontWeight="SemiBold"
                                   VerticalAlignment="Center"
                                   Padding="4,0"
                                   TextTrimming="CharacterEllipsis" />
                        <Button Grid.Column="1"
                                Tag="{x:Bind Id}"
                                Click="LaunchButton_Click"
                                VerticalAlignment="Center"
                                ToolTipService.ToolTip="Launch">
                            <FontIcon Glyph="&#xE768;" FontSize="16" />
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Window>
