<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="ace_run.ManageTagsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ace_run"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel MinWidth="380" Spacing="12">

        <!-- Toolbar -->
        <StackPanel Orientation="Horizontal" Spacing="8">
            <Button x:Name="NewTagButton" Click="NewTagButton_Click">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE710;" FontSize="14" />
                    <TextBlock x:Name="NewTagLabel" Text="New Tag" />
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- Empty state -->
        <TextBlock x:Name="EmptyHint"
                   Visibility="Collapsed"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   TextWrapping="Wrap" />

        <!-- Tag list -->
        <ListView x:Name="TagListView"
                  MinHeight="120"
                  MaxHeight="360"
                  SelectionMode="None">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem" BasedOn="{StaticResource DefaultListViewItemStyle}">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="8,4" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:TagViewModel">
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!-- Color dot -->
                        <Ellipse Grid.Column="0"
                                 Width="12" Height="12"
                                 Fill="{x:Bind ColorBrush, Mode=OneWay}"
                                 VerticalAlignment="Center" />

                        <!-- Tag name (inline editable) -->
                        <TextBox Grid.Column="1"
                                 Text="{x:Bind Name, Mode=OneWay}"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 Padding="4,2"
                                 VerticalAlignment="Center"
                                 LostFocus="TagName_LostFocus" />

                        <!-- Color picker -->
                        <ComboBox Grid.Column="2"
                                  MinWidth="120"
                                  VerticalAlignment="Center"
                                  Loaded="ColorCombo_Loaded"
                                  SelectionChanged="ColorCombo_SelectionChanged" />

                        <!-- Delete button -->
                        <Button Grid.Column="3"
                                Tag="{x:Bind}"
                                Click="DeleteTag_Click"
                                Padding="6,4"
                                ToolTipService.ToolTip="{x:Bind DeleteTooltip}">
                            <FontIcon Glyph="&#xE74D;" FontSize="14" />
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Inline new-tag form (collapsed by default) -->
        <Border x:Name="NewTagForm"
                Visibility="Collapsed"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,0"
                Padding="0,12,0,0">
            <StackPanel Spacing="10">
                <TextBlock x:Name="NewFormTitle"
                           Style="{StaticResource BodyStrongTextBlockStyle}" />
                <TextBox x:Name="NewNameBox" />
                <ComboBox x:Name="NewColorCombo"
                          SelectedIndex="0"
                          HorizontalAlignment="Left"
                          MinWidth="160" />
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button x:Name="ConfirmNewBtn"
                            Style="{StaticResource AccentButtonStyle}"
                            Click="ConfirmNewTag_Click" />
                    <Button x:Name="CancelNewBtn"
                            Click="CancelNewTag_Click" />
                </StackPanel>
            </StackPanel>
        </Border>

    </StackPanel>
</ContentDialog>
